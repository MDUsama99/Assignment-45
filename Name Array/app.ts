// Define an array of names
let names: string[] = ["Ali", "Bobby", "Chahal", "Dawood", "Saim"];

// Print each person's name using a for...of loop
for (let name of names) {
    console.log(name);
}